
# test handler.json - make sure all files can be located + functions are defined + descriptions are < 50 words.

# pull remote data into input folder as csv files

#test input columns against data_dictionary.json